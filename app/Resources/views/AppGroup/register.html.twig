{% extends 'base.html.twig' %}

{% block body %}

    {% include ('Shared/loginmenu.html.twig') %}

    <div class="row">
        <div class="h1">

        </div>

        <div class="col-md-3">
            {% include ('Shared/profilemenu.html.twig') %}
        </div>

        <div class="col-md-6 col-md-offset-3">

            <div class="well">

                {{ form_start(form, {'attr': {'class': 'bs-example form-horizontal', 'role': 'form'}}) }}
                <fieldset>
                    <legend>Ny grupp</legend>
                    {{ form_errors(form) }}

                    <div class="form-group">
                            {{ form_label(form.name, 'Namn', {'label_attr': {'class': 'col-md-2 control-label'}}) }}
                        <div class="col-md-10">
                            {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.name) }}
                        </div>
                    </div>

                    <div class="form-group">
                            {{ form_label(form.name, 'Beskrivning', {'label_attr': {'class': 'col-md-2 control-label'}}) }}
                        <div class="col-md-10">
                            {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': '10', 'cols': '30'}}) }}
                            {{ form_errors(form.description) }}
                        </div>
                    </div>
                </fieldset>

                <div class="row">
                    <div class="col-md-12">
                        <div class="pull-right">
                            <button type="Submit" class="btn btn-primary">Skicka</button>
                        </div>
                    </div>
                </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}