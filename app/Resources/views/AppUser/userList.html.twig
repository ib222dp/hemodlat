{% extends 'base.html.twig' %}

{% block body %}

    {% include ('Shared/loginmenu.html.twig') %}

    <div class="row col-md-6">
        <ul>
            {% for item in app_users %}
                <li><a href="{{ path('user_show', {'slug': item.id}) }}"> {{ item.username }} {{ item.firstName }}
                    {{ item.lastName }} {{ item.location.name }}
                </a></li>
            {% endfor %}
        </ul>
    </div>

{% endblock %}