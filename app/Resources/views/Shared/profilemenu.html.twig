<a href="{{ path('user_show', {'slug': app_user.id}) }}" class="h1">{{ app_user.userName }}</a>
<div class="h1">
    <img src="{{ app.request.scheme ~ '://' ~ app.request.host ~ '/' ~ app_user.profilePicture.webPath }}"
         class="img-circle img-responsive"/>
</div>
<div class="sidebar">
    <div class="list-group">
        <a href="{{ path('users_info', {'slug': app_user.id}) }}" class="list-group-item">Info</a>
        <a href="{{ path('users_groups', {'slug': app_user.id}) }}" class="list-group-item">Grupper</a>
        <a href="{{ path('users_crops', {'slug': app_user.id}) }}" class="list-group-item">Grödor</a>
        <a href="{{ path('friends', {'slug': app_user.id}) }}" class="list-group-item">Vänner</a>
    </div>
</div>